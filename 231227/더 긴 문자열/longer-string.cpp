#include <iostream>
#include <string>
using namespace std;

int main() {
    string str, str1;
    cin >> str >> str1;
    if(str.length() == str1.length()){
        cout << "same" ;
    }else{
        cout << "Coding 6";
    }

    return 0;

}